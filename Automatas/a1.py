import random
import math

alfabeto_1 = [0,1]


def func1(alfabeto, cantidad):
    count_long = 0
    
    # para agregar por nivel
    temp_long = []

    lenguaje_uni = []

    while count_long >= 0:
        # para cada longitud de palabra
        # crear el primero [elemento, indice]
        temp = [[alfabeto[0], 0] for x in range(count_long)]
        # concatena y agrega a la lista
        cadena = "".join([fila[0] for fila in temp])
        
        if 

        
